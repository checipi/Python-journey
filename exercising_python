# 01. Print the number, the previos number and the sum of the current number and previous number

sum_num = 0
nums = 0

for num in range(10):
    sum_num = nums + num
    print(f"Current number {num} Previous NUmber {nums} Sum {sum_num}")
    nums = num
    
# 02. Print characters present at an even index number

def odd(string):
    print("\n".join(string[::2]))
    
odd("PYnative")

